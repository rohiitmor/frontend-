{
  "name": "rentease",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "start": "python -m http.server 8000"
  },
  "devDependencies": {
    "@vercel/static-build": "^2.0.0"
  }
}
